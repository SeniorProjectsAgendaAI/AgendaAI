version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install uv
      - uv pip install --system --no-cache .
run:
  command: python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"